import React from 'react';
import { useAuth } from '../../Authentication/Authcontext';

function Homepage() {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
}

export default Homepage;
